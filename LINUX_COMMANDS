Basics

File system Hierarchy

📂/bin → Binaries (Essential User Commands)
	Contains basic command binaries needed by all users.
	Example: commands like ls, cp, mv, cat, echo, mkdir, etc. are stored here.
	These are "essential tools" — if /bin is broken, even the root user can’t perform basic operations.

📂 /etc → Configuration Files
		•	Contains system-wide configuration files (not user-specific).
    * Examples:
        * /etc/passwd → user account info
        * /etc/hostname → system hostname
        * /etc/resolv.conf → DNS settings
        * /etc/ssh/sshd_config → SSH server settings
    * Whenever you install software, most of its config files go inside /etc.
👉 Think of /etc as the "control center" of the system.

 📂 /var → Variable Data
		•	Contains files that change frequently.
    * Examples:
        *  /var/log/ → system logs (important for debugging, e.g., /var/log/syslog, /var/log/auth.log)
        * /var/spool/mail/ → emails
        * /var/cache/ → cache data
    * Think of /var as a place where constantly changing data is stored.
👉 If /var becomes full, services may crash because logs can’t be written.

📂 /tmp → Temporary Files
    * Stores temporary files created by users and apps.
    * Example: when you open a Word file or Chrome tab, temp data may be placed here.
    * Files inside /tmp are usually deleted on reboot.
    * 👉 Hackers sometimes use /tmp to execute scripts because it’s writable by everyone.

📂 /root → Root User’s Home Directory
    * This is the home directory of the root (superuser).
    * Equivalent to /home/root, but kept separate for security reasons.
    * Only root user has access to it.
👉 Think of it like Admin’s private room.


/
├── bin    → Essential user binaries (ls, cp, mv)
├── etc    → Configuration files
├── home   → Users’ home directories
├── var    → Logs, caches, spool data
├── tmp    → Temporary files
├── root   → Root user’s home
├── usr    → User programs and libraries
├── sbin   → System binaries (for admin tasks)
├── lib    → Shared libraries needed by system binaries
├── dev    → Device files (representing hardware, e.g., disks, USBs)
├── proc   → Virtual filesystem with system and process info
├── sys    → System info about devices, kernel
├── boot   → Bootloader files (kernel, initrd, GRUB)
├── opt    → Optional software (extra apps not part of system)
├── mnt    → Mount point for temporarily mounted filesystems
├── media  → Mount point for removable devices (USB, CD, etc.)

Basic Linux Commands
* Navigation: pwd, ls, cd
* File operations: touch, cp, mv, rm, cat, more, less, head, tail
* Directory operations: mkdir, rmdir, tree
* File content search: grep, find, locate
* Counting: wc, sort, uniq, cut
* Comparing: diff, cmp, comm





